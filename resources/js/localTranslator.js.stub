import {useTranslator} from "../../vendor/antonioprimera/laravel-js-localization/resources/js/InertiaPlugins/vue3Translator.js";

/**
 * Use the translator plugin to access the translation methods.
 * This is a convenience export, to avoid having to use the longer import statement.
 *
 * Usage (in your Vue3 components):
 *
 * //-------------------------------------------------------------------------
 * 		import useTranslator from '@/translator.js';		//import this file
 * 		const {txt, txts} = useTranslator();
 * //-------------------------------------------------------------------------
 *
 * Then you can use the txt and txts methods to translate strings.
 */
export default useTranslator;